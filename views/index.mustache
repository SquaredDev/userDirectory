<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/css/style.css" />
    <title>User Directory</title>
  </head>
  <body>
    <ul id="users">
      {{#users}}
        <li class="boxes">
          <a href="/user/{{username}}">
            <img src="{{avatar}}">
            <span class="name">{{name}}</span><br>
            {{^job}}
            <span class="job">Availible for hire</span>
            {{/job}}
            <span class="position">{{job}}<br>{{company}}</span>
          </a>
        </li>

      {{/users}}
    </ul>
  </body>

</html>
